[metadata]
name = llm-ida
version = 0.0.0
description = ML service for spatio-temporal time series (dev editable install)
author = 

[options]
packages = find:
install_requires =
    # runtime deps are optional and documented in requirements.txt
